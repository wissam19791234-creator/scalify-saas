'use client';import { AnimatePresence, motion } from 'framer-motion';import { useState } from 'react';
export default function FAQ(){const q=[['Faut-il savoir coder ?','Non.'],['Les sites sont-ils hébergés ?','Tu reçois le HTML.']];const [o,setO]=useState<number|null>(0);return <section id='faq' className='max-w-4xl mx-auto px-4 py-10'><h2 className='text-4xl mb-4'>FAQ</h2>{q.map((x,i)=><div key={x[0]} className='glass p-4 mb-2'><button onClick={()=>setO(o===i?null:i)} className='w-full text-left'>{x[0]}</button><AnimatePresence>{o===i&&<motion.p initial={{height:0,opacity:0}} animate={{height:'auto',opacity:1}} exit={{height:0,opacity:0}} className='text-[#8892a4] overflow-hidden mt-2'>{x[1]}</motion.p>}</AnimatePresence></div>)}</section>}
